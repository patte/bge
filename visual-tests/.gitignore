node_modules/
# transient captures (regenerated by the capture scripts on every run).
# NB: fixtures/ is the frozen original baseline and IS committed.
baseline/
new/
diffs/
# scratch capture dirs (before/after/stability comparisons)
before-fix/
after-fix/
stability-2/
*.log
.DS_Store
